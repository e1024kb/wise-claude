The ports do not vendor the Claude port's AGENTS.md, so the agent
roster pointer moves to the vendored agents/ directory. Applied to
the skill body AFTER the mechanical env-var rewrite; the find-text
must match exactly once.

<<<<<<<
`wise:code-reviewer`, … — see `${WISE_PLUGIN_ROOT}/AGENTS.md`). A
=======
`wise:code-reviewer`, … — see the role cards under
`${WISE_PLUGIN_ROOT}/agents/`). A
>>>>>>>
