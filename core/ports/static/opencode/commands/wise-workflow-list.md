---
description: "List every workflow available to the wise plugin — both the bundled defaults that ship with the plugin and the user-authored ones under the wise data dir's workflows/definitions/."
---

Load and follow the `wise-workflow-list` skill with arguments: $ARGUMENTS
