---
description: "First-time setup wizard — walk the user through installing wise's system deps (Python 3 + pyyaml/ulid/typing_extensions, Node ≥22, gh CLI + `gh auth login`) and cache the probe results so workflow runs skip the live check."
---

Load and follow the `wise-init` skill with arguments: $ARGUMENTS
