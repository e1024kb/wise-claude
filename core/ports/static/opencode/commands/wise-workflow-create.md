---
description: "Scaffold a new workflow through a step-by-step wizard — collects name, description, project-selection policy, pre-flight pins, inputs, and steps (id, type, type-specific fields, dependencies), previews the generated YAML + README, and writes on confirmation."
---

Load and follow the `wise-workflow-create` skill with arguments: $ARGUMENTS
