---
description: "Sweep every working-tree change into the index (`git add -A` — modifications, deletions, and new untracked files), draft a Conventional-Commits subject from the staged diff (Jira-scoped when a key is detectable from the branch / diff / log / session), and run `git commit`."
---

Load and follow the `wise-commit` skill with arguments: $ARGUMENTS
