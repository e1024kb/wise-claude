version: 2
name: direct-codex
description: One agent step on codex with the harness default model; drives the real-starter test.
steps:
  - id: answer
    type: agent
    harness: codex
    model: inherit
    prompt: "Answer. Reply with JSON only."
    schema: { type: object, properties: { answer: { type: string } }, required: [answer] }
    outputs: [answer]
